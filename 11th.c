//Quotient in whole Number When One Number Is Divided by Another.
#include <stdio.h>
int main()
{
    float a, b, quotient;
    printf("Enetr the value of a and bl");
    scanf("%f %f",&a,&b);
    quotient=a/b;
    printf("Quotient when %f is divided by %f is %f", a, b, quotient);
    return 0;
}